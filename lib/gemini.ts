const MODEL = 'gemini-2.0-flash';
function getKey() { const value = process.env.GEMINI_API_KEY; if (!value) throw new Error('GEMINI_API_KEY is not configured'); return value; }
export async function askGemini(prompt: string, json = false) { const response = await fetch('https://generativelanguage.googleapis.com/v1beta/models/' + MODEL + ':generateContent?key=' + getKey(), { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ contents: [{ parts: [{ text: prompt }] }], generationConfig: { temperature: 0.3, ...(json ? { responseMimeType: 'application/json' } : {}) } }) }); if (!response.ok) throw new Error('Gemini returned ' + response.status); const data = await response.json(); return data.candidates?.[0]?.content?.parts?.map((part: any) => part.text || '').join('') || ''; }
export function parseJson<T>(value: string) { const fence = String.fromCharCode(96).repeat(3); const cleaned = value.replace(new RegExp('^\\s*' + fence + '(?:json)?\\s*', 'i'), '').replace(new RegExp('\\s*' + fence + '\\s*$', 'i'), '').trim(); return JSON.parse(cleaned) as T; }
